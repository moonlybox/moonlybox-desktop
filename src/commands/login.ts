import type { CommandOptions } from '../lib/runner'
import { loadConfig } from '../lib/config'

/** Authorize via OAuth Device Flow（任务 3）（WBS 任务 3，骨架占位） */
export async function cmdLogin(args: string[], options: CommandOptions): Promise<void> {{
    // TODO(任务3): device code 请求 → 二维码展示 → 轮询 → 钥匙串存储
    console.log('login: not implemented yet (WBS task 3)')}}
